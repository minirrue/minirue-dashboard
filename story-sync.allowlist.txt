# story-sync.allowlist.txt — RULEBOOK §29
#
# Routes intentionally exempt from story-sync-check's MISSING/STALE gate. One surface token per
# line, exact match against "{app} {METHOD|PAGE} {path}". Blank lines and lines starting with #
# are ignored.
#
# Adding a line here is a conscious, reviewable diff — never a silent bypass. Do not add a route
# here just to make a failing check pass; add the story instead, unless the route genuinely has
# no story to cite.

# ================================================================================================
# PERMANENT — intentionally storyless (2026-07-09 full backfill pass, RULEBOOK §29 completed).
# The original 201-route backfill debt from §29's adoption day is now fully paid down (every route
# either got a citing story or is one of the genuine permanent exceptions below). Started at 201,
# now 3: dev-only internal tooling and an unauthenticated webhook, neither of which represents a
# real user-facing product capability that a story should describe.
# ================================================================================================

# Dev-only component-preview sandbox (app/_internal/preview) — internal tooling, not a real
# user-facing feature; no persona would ever have a story that says "as an X, I want to preview
# components."
minirue-dashboard PAGE /_internal/preview
minirue-frontend PAGE /_internal/preview

# Unauthenticated payment-gateway webhook (HMAC-verified in-handler, no JWT guard) — a
# machine-to-machine integration endpoint, not a user-facing route any persona interacts with.
minirue-backend POST /v1/webhooks/payments/gateway

# ================================================================================================
# 2026-07-22 — Global variants screen, specs/2026-07-22-global-variants-design.md
#
# Allowlisted because the story vault this page would cite no longer exists in this repo:
# apps/minirue-obsidian was emptied on 2026-07-22 and the "vault is upstream of code" standard
# was dropped by the project owner. There is no {project}-us story to write, so the MISSING gate
# has no way to be satisfied for any route added from this date forward.
#
# This is a stopgap, not a verdict. If story-sync is meant to keep running, it needs a source of
# truth that still exists; if it is not, the workflow and this file should be removed outright.
# Flagging rather than deciding — that call belongs to the project owner.
# ================================================================================================
minirue-dashboard PAGE /dashboard/products/global-variants
minirue-dashboard PAGE /dashboard/products/option-lists

# 2026-07-23 — Accounts screen (super admin only),
# specs/2026-07-23-account-administration. Same vault-is-gone reason as above.
minirue-dashboard PAGE /dashboard/admin

# 2026-07-23 — notification feed page, from the concurrent session's work, not
# this one's. Listed only so the shared pre-push hook does not block either of
# us; same vault-is-gone reason as above.
minirue-dashboard PAGE /dashboard/notifications
# /dashboard/products/overview line removed (2026-07-30, Task 5a) — the
# Catalogue Overview tab and the page behind it were deleted outright.

# 2026-07-23 — partner-oversight page, also from the concurrent session's work, not
# this one's. Listed only so the shared pre-push hook does not block either of us.
minirue-dashboard PAGE /dashboard/partners

# 2026-07-24 — customer support inbox (this session). Team + collab support
# screens. Story pending (vault decoupled, same reason as everything above).
minirue-dashboard PAGE /dashboard/support
minirue-dashboard PAGE /dashboard/collab/support

# 2026-07-29 — the dashboard half of "spaces": a partner managing their own category tree, the
# screen behind /v1/collab/categories. Exempted as a block with the backend routes it calls, for
# the same reason — new product surface with no existing story that describes it, and inventing
# an attribution would record coverage that does not exist. Story pending per §18/§21 (vault
# decoupled).
minirue-dashboard PAGE /dashboard/collab/categories

# 2026-07-29 — the Reviews moderation screen, where an admin approves or rejects what customers
# write about a product. Exempted alongside the backend review routes it calls, for the same
# reason: new product surface with no existing story describing it. Story pending per §18/§21
# (vault decoupled).
minirue-dashboard PAGE /dashboard/reviews

# ================================================================================================
# PERMANENT — analytics screens (2026-07-31).
#
# No citing story because the story project is gone: minirue-us was deleted and is not returning,
# so there is nowhere for one to live. The header's "genuinely has no story to cite" case, not a
# way to quiet a failing check. What these screens do is written down instead in
# docs/superpowers/runbooks/analytics-*.md and in the Settings -> Info changelog entry.
#
# All nine sit under /analytics for the operator (lib/auth/roles.ts folds them to the ADMIN_ONLY
# guard on the parent). If a story project is ever reinstated, these belong in it.
# ================================================================================================
minirue-dashboard PAGE /dashboard/analytics/realtime
minirue-dashboard PAGE /dashboard/analytics/visitors
minirue-dashboard PAGE /dashboard/analytics/visitors/:param
minirue-dashboard PAGE /dashboard/analytics/pages
minirue-dashboard PAGE /dashboard/analytics/products
minirue-dashboard PAGE /dashboard/analytics/products/:param
minirue-dashboard PAGE /dashboard/analytics/acquisition
minirue-dashboard PAGE /dashboard/analytics/checkout
minirue-dashboard PAGE /dashboard/analytics/events

# The Sales tab (2026-07-31) — the original analytics screen, preserved as its
# own tab rather than replaced. Same reason as the other analytics entries:
# minirue-us is deleted, so no citing story can exist.
minirue-dashboard PAGE /dashboard/analytics/sales

# The Capacity tab (2026-08-01) — server headroom, not shop traffic. Same
# reason as every other analytics entry above: minirue-us is deleted, so no
# citing story can exist to point at. Reviewed and intentional.
minirue-dashboard PAGE /dashboard/analytics/devops
