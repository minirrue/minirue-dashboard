import RealtimeClient from './RealtimeClient';

export const metadata = {
  title: 'Realtime — Analytics — MiniRue Admin',
};

export default function AnalyticsRealtimePage() {
  return <RealtimeClient />;
}
