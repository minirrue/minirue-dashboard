import EventsExplorerClient from './EventsExplorerClient';

export const metadata = {
  title: 'Events — Analytics — MiniRue Admin',
};

export default function AnalyticsEventsPage() {
  return <EventsExplorerClient />;
}
