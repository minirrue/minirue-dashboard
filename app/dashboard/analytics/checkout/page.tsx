import CheckoutFunnelClient from './CheckoutFunnelClient';

export const metadata = {
  title: 'Checkout — Analytics — MiniRue Admin',
};

export default function AnalyticsCheckoutPage() {
  return <CheckoutFunnelClient />;
}
