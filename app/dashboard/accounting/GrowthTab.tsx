import BeingBuilt from './BeingBuilt';

export default function GrowthTab() {
  return (
    <BeingBuilt title="Growth">
      Order profit, what free delivery would have to achieve, ad spend per channel with the cost of
      each new customer, and a tagged link builder for ads.
    </BeingBuilt>
  );
}
