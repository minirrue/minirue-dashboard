import AdminAccountsClient from './AdminAccountsClient';

export const metadata = {
  title: 'Accounts — MiniRue Admin',
};

export default function AdminAccountsPage() {
  return <AdminAccountsClient />;
}
