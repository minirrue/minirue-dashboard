import CollaboratorsClient from './CollaboratorsClient';

export const metadata = {
  title: 'Collaborators — MiniRue Admin',
};

export default function CollaboratorsPage() {
  return <CollaboratorsClient />;
}
