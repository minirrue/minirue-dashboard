import CollaboratorDetailClient from './CollaboratorDetailClient';

export const metadata = {
  title: 'Collaborator — MiniRue Admin',
};

export default function CollaboratorDetailPage() {
  return <CollaboratorDetailClient />;
}
