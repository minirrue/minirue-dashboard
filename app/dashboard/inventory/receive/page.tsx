import ReceiveStockClient from './ReceiveStockClient';

export const metadata = {
  title: 'Receive Stock — MiniRue Admin',
};

export default function ReceiveStockPage() {
  return <ReceiveStockClient />;
}
