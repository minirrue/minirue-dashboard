import StorefrontAppearanceClient from './StorefrontAppearanceClient';

export const metadata = {
  title: 'Storefront — MiniRue Admin',
};

export default function StorefrontAppearancePage() {
  return <StorefrontAppearanceClient />;
}
