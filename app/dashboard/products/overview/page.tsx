import CatalogOverviewClient from './CatalogOverviewClient';

export const metadata = {
  title: 'Catalogue — MiniRue Admin',
};

export default function CatalogOverviewPage() {
  return <CatalogOverviewClient />;
}
